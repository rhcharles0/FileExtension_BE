name: CI/CD Build Workflow

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20' # 프로젝트에 맞는 Node.js 버전으로 설정
          cache: 'pnpm'      # pnpm 캐시 사용 설정

      - name: 📥 Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 8
          run_install: false # 기본적으로 install을 실행하지 않도록 설정

      - name: ⚙️ Install dependencies
        run: pnpm install --frozen-lockfile
        
      - name: 🚀 Run pnpm dlx prisma generate
        run: pnpm dlx prisma generate

      - name: 🏗️ Run Next.js build
        run: pnpm run build

      - name: ⬆️ Upload Next.js build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: nextjs-build
          path: .next
